const txtName = document.getElementById('txtName');
const txtEmail = document.getElementById('txtEmail');
const txtMesseger = document.getElementById('txtMesseger');
const btnSubimit = document.getElementById('btnSubimit');

btnSubimit.addEventListener('click', () =>{
  const subscription = {
    name: txtName.value,
    email: txtEmail.value,
    messeger: txtmesseger.value,
  }
  
  
  
})